#include "main.h"
#include <stdio.h>

/**
 * mul - Is a function that multiplies two integers.
 * @a : this will be multiplied with b.
 * @b : this will be multiplied with a.
 * Return: Always 0.
 */
int mul(int a, int b)
{
return (int a * int b);
}
